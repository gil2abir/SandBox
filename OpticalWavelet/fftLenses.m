function [outputImage] = fftLenses(inputImage, modelParameters)
%FFTLENSES takes an inputImage, 2-lenses free space propagation model parameters
%          performing an fft simulation and resulting the transformed
%          image.

end

